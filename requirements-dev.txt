# Dev/test dependencies for wallet-helper. The library itself needs nothing at
# runtime (stdlib only) — see requirements.txt and pyproject.toml
# [project.optional-dependencies].dev.
pytest>=8
ruff>=0.6
# Exercises the optional surfaces in the test suite (both are optional extras of
# the library, pulled in here so CI can test them; core users need neither).
click>=8.1,<9      # click CLI variant (wallet-helper[cli])
httpx>=0.27        # FastAPI TestClient transport for api tests (wallet-helper[api])
