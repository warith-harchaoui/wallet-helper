# Dev and test dependencies for wallet-helper. The runtime dependency (os-helper)
# lives in requirements.txt and pyproject.toml.
pytest>=8
ruff>=0.6
# Exercise the optional surfaces in the test suite (both are optional extras of
# the library, pulled in here so CI tests them; core users need neither).
click>=8.1,<9      # click CLI variant (wallet-helper[cli])
httpx>=0.27        # FastAPI TestClient transport for the api tests (wallet-helper[api])
